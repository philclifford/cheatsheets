---
tags: [ virtualization ]
---

# show usage and all the optional parameters
quickemu

# To run a vm
quickemu (--vm <path-to-vm.conf>)

# Delete the disk image and EFI variables
# The disk will be recreated empty on next run
quickemu (--vm <path-to-vm.conf>) --delete-disk

# Delete the entire VM and it's configuration
quickemu (--vm <path-to-vm.conf>) --delete-vm

# To run a vm with a spice display
quickemu (--vm <path-to-vm.conf>) --display spice

# To run a vm with a braille terminal display - Version > 3.11 and working brltty required
quickemu (--vm <path-to-vm.conf> --accessible braille --display sdl)
  #   or
quickemu (--vm <path-to-vm.conf> --acc brl --display sdl)

